export interface Jogo {
    id ?: number,
    nome: string,
    desenvolvedora: string,
    imagem: string,
    lancamento: string,
    genero: string,
    created_at?: string,
    update_at?: string
}